## Masalah
- Vite gagal me-resolve `canvas-confetti` di `CBTGameContainer.tsx` karena dependency belum terpasang.

## Rencana Perbaikan
1. Pasang paket `canvas-confetti` di project (`npm i canvas-confetti`).
2. Tidak mengubah kode apa pun—tetap sesuai referensi.
3. Verifikasi instalasi dengan daftar dependency (`npm ls canvas-confetti`).

## Dampak
- Menghilangkan error build dan mengaktifkan efek konfeti saat kemenangan di game CBT.

Saya akan memasang paket sekarang dan memverifikasinya.