import React from "react";
// Import CSS globals from the reference so visuals match exactly
import "../duo_ref/app/globals.css";
// Parallax support styles
import "../duo_ref/app/parallax.css";
// Import the reference landing page component
import HomePage from "../duo_ref/app/page";

export default function LandingPage() {
  return <HomePage />;
}