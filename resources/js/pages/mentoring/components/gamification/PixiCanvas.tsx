import React from 'react';

export default function PixiCanvas() {
  // Stub component to satisfy build; actual implementation not needed for landing page
  return <div style={{ display: 'none' }} />;
}